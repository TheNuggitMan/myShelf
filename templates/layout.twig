<!DOCTYPE html>
<html lang="en" class="{% block class %}{% endblock %}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" href="fav/favicon.ico">
  <link rel="apple-touch-icon" sizes="180x180" href="fav/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="32x32" href="fav/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="fav/favicon-16x16.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@600...700&display=swap" rel="stylesheet">
  <title>{% block title %}myShelf{% endblock %}</title>
  <script src="js/burger.js" defer></script>
</head>
<body>
<header>
  <div><a href="/" title=""><img src="/images/myShelf-trans.webp" alt="my_Shelf-logo"></a></div>

    <!-- Burger -->
  <button class="nav-toggle" aria-controls="site-menu" aria-expanded="false">
    <span class="sr-only">Menu</span> ☰
  </button>

  <nav id="site-menu" class="menu" aria-label="Primary">
    <a href="/" class="{{ currentPath == '/' ? 'active' : '' }}">Home</a>
    <a href="/myshelf" class="{{ currentPath == '/myshelf' ? 'active' : '' }}">myShelf</a>
  </nav>
</header>
<main>
  {% block content %}{% endblock %}
</main>
<footer>
  <small>© {{ "now"|date("Y") }} myShelf
    <div>
      {% if loggedIn %}
        <a href="/admin" class="{{ currentPath == '/admin' ? 'active' : '' }}">Admin</a>
        <form action="/logout" method="post" style="display:inline">
          <button class="btn" type="submit">Log out</button>
        </form>
      {% else %}
        <a href="/login">Log in</a>/<a href="/register">Register</a>
      {% endif %}
    </div>
  </small> 
</footer>
</body>
</html>